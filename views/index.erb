<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<title>アイデアメモリー</title>
</head>
<body>
	<h1>アイデアメモリー</h1>
<% unless session[:user] %>
	<a href="/signin">ログイン</a><a href="/signup">新規登録</a><br>
	<h2>現在の状態：未ログイン</h2>
<% else %>
    <a href="/signout">ログアウト</a>
    <h2><%= User.find(session[:user]).mail %></h2>
 
    <a href="/user_workspaces/<%= User.find(session[:user]).id %>">マンダラート機能</a><a href="/user_memos/<%= User.find(session[:user]).id %>">メモ機能</a><br>
<% end %>
</body>
</html>
